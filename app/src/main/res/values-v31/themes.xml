<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.OffLine_HouseholdBook" parent="Theme.MaterialComponents.DayNight.NoActionBar">
    </style>

    <style name="Theme.App.SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- 배경 색상 -->
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/black</item>
        <!-- 스플래시 화면 아이콘 -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/household_2</item>

        <!-- 아이콘 애니메이션 지속 시간 -->
        <item name="windowSplashScreenAnimationDuration">5000</item>

        <!-- 다음에 적용될 테마-->
        <item name="postSplashScreenTheme">@style/Theme.OffLine_HouseholdBook</item>
    </style>





</resources>